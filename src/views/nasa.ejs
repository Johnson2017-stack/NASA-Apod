<html>

<head>
  <link href="./public/style.css" rel="stylesheet" type="text/css" />
</head>

<body>
  <div>
    <h1 id="top-title">Astronomy Picture of the Day</h1>

    <h2 id="title"><%= nasaData.title %></h2>
    <h3 id="date"><%= nasaData.date %></h3>
  </div>
  <div id="media">
    <%if(nasaData.media_type === "video") { %>
    <iframe width="420" height="315" src="<%=nasaData.url%>"> </iframe>
    <% } else { %>
    <img src="<%=nasaData.url%>" />
    <% } %>
  </div>
  <p><%= nasaData.explanation %></p>
  <div class="stars"></div>
  <div class="twinkling"></div>
  <div class="clouds"></div>
</body>

</html>